export interface UserConfiguresType {
    id?: number
    uId?: number // 用户id
    configureName?: string // 配置名
    configureValue?: string // 配置值
    type?: number // 类型
    status?: number // 状态
    remarks?: string // 备注
    createdAt?: string
    updatedAt?: string
    endTime?: string // 到期时间
}